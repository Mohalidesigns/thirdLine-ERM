<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!-- The suite peaks around 150 MB, and the WP-04 performance test
             builds a 5,000-risk register in memory to prove the 12-period
             trend query does not scale with the row count. PHP's 128M default
             is below both, so the limit is set here rather than left to
             whatever php.ini the CI image ships with. -->
        <ini name="memory_limit" value="512M"/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!--
            MySQL, not SQLite in memory. This project runs MySQL in
            development and in production, and a suite that proved itself on a
            driver nobody deploys proves the wrong thing: P0 shipped two index
            names MySQL rejected and SQLite had accepted, and only a MySQL run
            found them.

            It costs speed — the suite is minutes rather than ninety seconds —
            and it means a MySQL server is now a prerequisite for running the
            tests. `DB_DATABASE` is deliberately a SEPARATE database from the
            development one, because RefreshDatabase drops every table it
            finds: point this at `risk` and the first test run destroys the
            developer's working data.
        -->
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_HOST" value="127.0.0.1"/>
        <env name="DB_PORT" value="3306"/>
        <env name="DB_DATABASE" value="risk_test"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
        <!-- The licensing gate is exercised explicitly by tests/Feature/Licensing;
             every other feature test hits real routes unlicensed. -->
        <env name="LICENSE_ENFORCE_VALID" value="false"/>
    </php>
</phpunit>
