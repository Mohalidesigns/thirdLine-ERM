{
    "name": "@thirdline/ui",
    "version": "0.1.0",
    "description": "The shared React interface layer for the ThirdLine GRC products: primitives, layouts, the data grid, the widget engine and the metadata-driven form.",
    "license": "UNLICENSED",
    "private": true,
    "type": "module",
    "main": "src/index.js",
    "exports": {
        ".": "./src/index.js",
        "./css/*": "./css/*",
        "./*": "./src/*"
    },
    "sideEffects": [
        "*.css"
    ],
    "peerDependencies": {
        "@headlessui/react": "^2.0.0",
        "@inertiajs/react": "^2.0.0",
        "chart.js": "^4.0.0",
        "gridstack": "^13.0.0",
        "react": "^18.0.0",
        "react-dom": "^18.0.0"
    },
    "peerDependenciesMeta": {
        "chart.js": { "optional": true },
        "gridstack": { "optional": true }
    },
    "scripts": {
        "test": "echo \"no JS test runner in this repository yet; the consuming application's build and its Inertia page tests are the verification\" && exit 0"
    }
}
